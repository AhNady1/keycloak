apiVersion: v1
kind: Service
metadata:
  name: postgresql-db-service
  namespace: redhat-keycloak-operator
spec:
  clusterIP: None
  selector:
    app: postgresql-db
  ports:
    - name: postgresql
      port: 5432
      targetPort: 5432
